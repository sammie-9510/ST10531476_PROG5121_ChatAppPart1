# Chat Application Part 1

## Student Information
- Name: Esamisipho Ndiko
- Student Number: ST10531476
- Module: PROG5121

---

## Project Information

In this project we were tasked to make a chat app login. We had to create our own package which wa sname chat app part 1, in the package we had to creat 2 classes,
one was named Login and the other MainApp. The login class we had to create a login section for the user to access the app but first we had to make theuse register
first. In oder to register had codittions set for them in order to register successfully. First the user username, the username had to be 5 characters long and 
contain a undercase. If the user did not use in any of the condition they will be informed that the username is not correctly formatted and to try again.
had to create a password, the password had be 8 characters long, have a capital letter, atleast 1 number, and a special character e.d #, @, & etc. If the user did not
put any of this values in then they will be informed that the password is not correctly formatted and to try again. Then the user had to put in their phone munber, 
the phone number had to contain the South African international code (+27) and be 12 characters long, if the condidtion is not meet they user will be infromaed
they incorrectly put the phone number is not correctly formatted and to try again. Once successfully registered the user will be able to login with their username
and password if the login is incorrect the user will be meet with a try again message if successfully logged in they will be met witha welcome message.

We move to our 2nd class which is the main app where we get the users input by using Jave Scanner.This helps the user to actually put in their input because the 
scanner class is used to get the users input. We first get the user to register with all the conditions from the login put in place and once the user is successfully
register they are to login using their username and password, then will get a welcome message. If registering was unsuccessful then conditions were not met and the
user needs to try again and double check they get the proper format. Now if loginning in was successful the user will be met with a try again message and the user
needs to check they put in the correct username and password.

After we are done with MainApp class and Login class. We had to test the code. So went to our test and created a package with same name so chat app part 1, then
create a class named LoginTest, In this class we test the code using Junit testing. Junit is used for unit testiong of applications helps test components of code
to make sure they work correctly. We tested our username, password and phone number using assertion to test the truth values if evaluation of the boolean value is not true than values is false. We use asertTrue assertFalse or assertEquals, at the end your test has to be 100% pass.

---
